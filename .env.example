# Copy this file to .env and fill in your values.
# .env is gitignored and must never be committed.

# OpenRouter API key — used for Grok and Qwen inference
OPENROUTER_API_KEY=sk-or-v1-...

# Weights & Biases API key — used for finetuning run tracking
WANDB_API_KEY=...
